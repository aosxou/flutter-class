# 앱 실행 구조
## main.dart
```dart
WidgetsFlutterBinding.ensureInitialized();
prefs = await SharedPreferences.getInstance();
```
# 기능
SharedPreferences 초기화

앱 설정 정보 유지

## App 진입 처리 (Init / Splash 분기)
```dart
bool isInitStarted = prefs.getBool('isInitStarted') ?? true;

값	이동 화면
true	InitStartPage
false	SplashPage
```

# 하단 네비게이션 바 구조

```dart
root.dart
TabBarView(
  children: [
    HomePage(),
    동네생활,
    내 근처,
    채팅,
    나의 밤톨
  ]
)
```

## BottomNavigationController

menuIndex 상태 관리

TabController 연동

화면탭 변경 처리

# 홈 화면 구조

HomeController

ProductRepository 통한 상품 데이터 관리

Firebase Storage 연동

## 상품 등록 기능

ProductWriteController 역할

초기 구성
```dart
ProductWriteController(
  UserModel owner,
  ProductRepository,
  CloudFirebaseRepository
)
```
## 상품 유효성 검사
```dart
if (사진 && 가격 >= 0 && 제목 != '') {
  등록 가능
}
```

 등록 버튼은 조건 만족 시 활성화

## 이미지 선택

photo_manager 라이브러리 활용

최대 10장

선택, 삭제 가능
```dart
RxList<AssetEntity> selectedImages;
```
💰 가격 & 무료 나눔
productPrice ≥ 0
isFree == true → price = 0

## 카테고리 선택
```dart
ProductCategorySelector 사용
```

## 거래 희망 장소 설정

지도 UI 제공

위치 선택 후 저장

wantTradeLocation
wantTradeLocationLabel


## 상품 최종 등록

submit()

처리 과정

product 객체 생성

Firestore 저장

등록 완료 알림

# 공통 UI 관리
```dart
AppFont
class AppFont extends StatelessWidget
```
# 공통 폰트 적용

색상, 크기, 굵기 커스텀

## 사용 라이브러리
| 구분       | 라이브러리              |
| -------- | ------------------ |
| 상태 관리    | get                |
| Firebase | Storage            |
| 이미지      | flutter_svg        |
| 갤러리      | photo_manager      |
| 지도       | flutter_map        |
| 위치       | geolocator         |
| 로컬 저장    | shared_preferences |
| 해시       | crypto             |
| UUID     | uuid               |


## 프로젝트 기능 요약

| 기능         | 구현                |
| ---------- | ----------------- |
| 로그인 / 로그아웃 | ✅                 |
| 상품 등록      | ✅                 |
| 이미지 업로드    | ✅                 |
| 거래 위치 선택   | ✅                 |
| 하단 네비      | ✅                 |
| 상태 관리      | GetX              |
| 초기 설정 저장   | SharedPreferences |